import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // appDir đã được tích hợp sẵn trong Next.js 15, không cần khai báo experimental
  // Các cấu hình khác nếu có
};

export default nextConfig;